#include <stdio.h>
#include <stdlib.h>
/*
 *7-print_tebahpla.c
 * */

int main(void)
{
        char alphabet = 'z';

        while(alphabet >= 'a')
        {

                putchar(alphabet);
                alphabet--:
        }

        putchar('\n');
        return (0);
}
